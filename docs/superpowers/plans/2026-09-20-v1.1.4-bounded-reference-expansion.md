# v1.1.4 Bounded Reference Expansion Implementation Plan

Execution: inline, no subagents, as explicitly requested by the user.

Goal: implement the approved bounded expansion and reproducible compatibility spec.
Architecture: pure policy helpers, SQLite-backed run snapshots and normalized
history, a bounded orchestrator exposed through Workspace, MCP and CLI.
Tech stack: existing Python, SQLite, pytest, MCP and arXiv acquisition stack.

## Constraints

Default depth 2, new papers 10, searches 100, edges 500. Explicit advance runs
automatic imports under `unique_strong_v1`. Preserve existing APIs and user files.
Only arXiv and user-supplied PDFs are importable. No subagents. Push the tested
feature branch; no direct main merge or automatic GitHub release.

## Tasks

- [x] Policy and fixture contracts: add `tests/test_reference_expansion.py` with
  exact-identifier, corroborated metadata, conflict, budget and resume cases.
  Run `uv run pytest tests/test_reference_expansion.py -q` and observe missing
  APIs. Implement `reference_expansion_policy.py` pure `validate_policy` and
  `select_candidate` returning evidence-backed eligibility.
- [x] Storage and execution: add `reference_expansion_store.py`, schema 8 migration,
  run/node/edge/attempt/event records and workspace lease. Implement
  `reference_expansion.py` Workspace mixin with create/advance/get/list/decide/
  update/pause/cancel/export. Tests assert e.g.
  `assert resumed['usage']['new_papers'] == 1` after interrupted import recovery
  and `assert len(resumed['nodes']) == 2` after duplicate target discovery.
- [x] Public surfaces and reports: `reference_expansion_report.py` renders saved
  graphs and boundaries; `reference_expansion_api.py` registers MCP and CLI
  entry points. Integrate additive reading summaries. Test subprocess stdio
  MCP and CLI against real temporary workspaces, plus overwrite refusal.
- [x] Compatibility fixtures/docs: checked-in MIT synthetic source set and
  golden outputs, provider/acquisition injection only, network-denying tests.
  Add pinned client recipes and honest documented/not-run compatibility matrix.
- [x] Release validation: update active version pins/tests to 1.1.4, run focused
  suite then full suite and build/install smoke. Self-review diff against spec,
  correct failures. Delivery after validation: commit only task files, push the
  feature branch and verify its remote commit SHA.

Validation commands (repository root):

```powershell
uv run pytest tests/test_reference_expansion.py -q -p no:cacheprovider
uv run pytest -q -p no:cacheprovider --basetemp .pytest-v114-final
uv build
git diff --check
git push -u origin feature/v1.1.4-bounded-reference-expansion
```

Expected: all deterministic tests pass, wheel/sdist build successfully, no diff
whitespace errors, remote branch equals the locally verified commit.

## Verification record — 2026-09-20

Execution and self-review were single-agent, honoring the user's explicit request.
Reviewed against the approved spec, including automatic-selection boundaries,
durable reservations, migration rollback, source replacement, immutable candidate
snapshots, API parity and actual-client evidence separation.

- Windows, Python 3.13.13, MCP SDK 2.1.1.
- Final full suite: `uv run pytest -q -p no:cacheprovider --basetemp
  .pytest-v114-release-verified` — **548 passed, 1 skipped**.
- `uv build` — wheel and sdist built successfully; inspected sdist contains no
  temporary test directories, session logs or workspace databases.
- Isolated wheel installation: `uv run --isolated --no-project --with
  ./dist/papergraph_mcp-1.1.4-py3-none-any.whl --with pytest pytest
  tests/test_reference_expansion_api.py -q -p no:cacheprovider --basetemp
  .pytest-v114-wheel` — **3 passed** (CLI, reports, real MCP stdio subprocess).
- Installed-wheel `--version` — `papergraph-mcp 1.1.4`; `doctor` — no warnings.
- Golden fixture regeneration matched deterministic JSON/Markdown expectations.
- `git diff --check` passed before staging; repeat against the staged files.

Review fixes included transactional migration rollback, refresh candidate-ID
ownership, final-step control signals, stale sources, PDF path-based identity
checks, cumulative reserved budgets, and a reproduced race where another client
imported the same paper during acquisition. Expansion paper writes now fence
the lease and check identity/existence inside the write transaction; commit
markers roll back with the paper and support safe interruption reconciliation.

The existing LaTeX citation-record path was added to paper import plans because
otherwise source fixtures exposed no outbound references. This preserves
existing key/file evidence; it does not add general bibliography text recovery.
Golden source revision hashes are timestamp-derived and normalized explicitly.

Codex, Claude and AutoClaw actual-client runs remain `documented_not_run`.
No tag, main-branch merge or GitHub Release is part of this delivery. Pre-existing
changes under `docs/sessions` are excluded from the development commit.
